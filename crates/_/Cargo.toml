[package]
name = "spitfire"
version = "0.6.0"
edition = "2021"
authors = ["Patryk 'PsichiX' Budzynski <psichix@gmail.com>"]
description = "Spitfire toolset"
license = "MIT OR Apache-2.0"
homepage = "https://github.com/PsichiX/spitfire"
repository = "https://github.com/PsichiX/spitfire"
documentation = "https://docs.rs/spitfire"
readme = "../../README.md"

[features]
glow = ["spitfire-glow"]
fontdue = ["spitfire-fontdue"]

[dependencies]
spitfire-core = { version = "0.6", path = "../core" }
spitfire-glow = { version = "0.6", path = "../glow", optional = true }
spitfire-fontdue = { version = "0.6", path = "../fontdue", optional = true }

[dev-dependencies]
spitfire-core = { path = "../core" }
spitfire-glow = { path = "../glow" }
spitfire-fontdue = { path = "../fontdue" }
png = "0.17"
bytemuck = { version = "1", features = ["derive"] }
fontdue = "0.7"
